# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  name: Default
steps:
- task: CmdLine@2
  inputs:
    script: |
      echo Write your commands here
      echo Hello world
      $(APIGWDEPLOYTOOLS)/apigateway/posix/bin/projpack --create  --passphrase-none --name=dev --type=pol --add $(Agent.BuildDirectory)"/s/APIProject11" --projpass-none --add $(Agent.BuildDirectory)"/s/commonProject" --projpass-none --add $(Agent.BuildDirectory)'/s/APIProject22' --projpass-none
    workingDirectory: '$(HOME)'
- script: echo Hello, world!
  displayName: 'Run a one-line script'
